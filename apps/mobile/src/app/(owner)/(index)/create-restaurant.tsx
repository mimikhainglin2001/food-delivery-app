import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const CreateRestaurant = () => {
  return (
    <View>
      <Text>CreateRestaurant</Text>
    </View>
  );
};

export default CreateRestaurant;

const styles = StyleSheet.create({});
